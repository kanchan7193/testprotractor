# testprotractor

```
sudo snap install docker
image use from here
https://github.com/seleniumhq/docker-selenium/releases
npm install docker
sudo docker ls
sudo docker rm -f {processid}
sudo docker run -d -p 4444:4444 selenium/standalone-chrome:3.141.59-neon
npm run test
```